name: tello
description: "Разработка мобильного приложения для прослушивания подкастов."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  audioplayers: ^6.8.0
  file_picker: 10.3.10
  flutter:
    sdk: flutter
  http: ^1.6.0
  http_parser: ^4.1.2
  provider: ^6.1.5+1
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true